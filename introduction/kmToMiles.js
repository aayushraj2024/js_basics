// Program to convert kilometer into miles

// 1 km = 0.621371 miles
const factor = 0.621371;

const kilometers = 12;

const miles = kilometers * factor;

console.log(`${kilometers} kilometers is equal to ${miles.toFixed(4)}.`);
