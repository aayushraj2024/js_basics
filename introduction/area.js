// program to calculate area of triangle

const base = 10;
const height = 20;

const area = (base * height) / 2;

console.log(`area of triangle having base = ${base}, height = ${height} is ${area} sq. unit`);